@model RefreshMyStyleApp.Models.ApplicationUser

@{
    ViewData["Title"] = "SearchUsers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>SearchUsers</h1>
<div class="container-fluid">
    <div class="row">
        @foreach (var user in Model.SearchResults)
        {
            <div>
                @*<form asp-action="SearchUser" asp-controller="ApplicationUser">*@
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                @*<input type="hidden" asp-for="SentFriendRequests" />*@
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="ProfileImageName" />
                <div class="form-group">
                    <label asp-for="SearchUsers" class="control-label"></label>
                    <input name="searchString" type="text" class="form-control" />
                    @*<span asp-validation-for="ApplicationUser.Friends" class="text-danger"></span>*@
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
                @*</form>*@
            </div>
            @if (Model.SearchUsers.Any())
            {

                <div>

                    <hr />
                    <dl class="row">
                        @*<div>
                            <img src="~/images/profileImages/@Model.ProfileImageName" />
                        </div>*@
                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(model => model.FName)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(model => model.FName)
                        </dd>
                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(model => model.LName)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(model => model.LName)
                        </dd>
                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(model => model.FullName)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(model => model.FullName)
                        </dd>
                        <dt class="col-sm-2" style="display: none">
                            @Html.DisplayNameFor(model => model.IdentityUser)
                        </dt>
                        <dd class="col-sm-10" style="display: none">
                            @Html.DisplayFor(model => model.IdentityUser.Id)
                        </dd>
                    </dl>
                </div>
            }
        }


    </div>

</div="">



<div>
    <a asp-action="Edit">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
